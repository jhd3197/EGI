<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.egi.app">

    <!-- Internet sync -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Bluetooth Low Energy -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <!-- Location is required for BLE scan on older Android versions -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Wi-Fi Direct bulk-sync fallback -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.NEARBY_WIFI_DEVICES"
        android:usesPermissionFlags="neverForLocation" />

    <!-- SMS fallback (Phase 7): receive structured EGI check-in texts offline.
         RECEIVE_SMS is a sensitive runtime permission the user must explicitly
         grant; this is an OPTIONAL fallback path and the app works without it. -->
    <uses-permission android:name="android.permission.RECEIVE_SMS" />

    <!-- Foreground service that keeps the mesh relaying while backgrounded -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <!-- Ongoing mesh notification (runtime-requested on API 33+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <uses-feature android:name="android.hardware.bluetooth_le" android:required="true" />
    <uses-feature android:name="android.hardware.wifi.direct" android:required="false" />
    <!-- RECEIVE_SMS implies telephony hardware; mark it optional so Chromebooks and
         tablets without telephony can still install EGI for the mesh/PWA. -->
    <uses-feature android:name="android.hardware.telephony" android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EGI"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="false">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".MeshForegroundService"
            android:foregroundServiceType="connectedDevice"
            android:exported="false" />

        <!-- Native FCM client (Phase 6). Optional: only fires when a real
             google-services.json is present (the google-services Gradle plugin is
             applied conditionally in app/build.gradle). The firebase-messaging
             library declares its own internal receiver; we only register our
             FirebaseMessagingService subclass here. POST_NOTIFICATIONS (declared
             above) covers any notifications it surfaces. -->
        <service
            android:name=".push.MeshFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- SMS fallback receiver. Must be exported to receive the system broadcast,
             and guarded by BROADCAST_SMS so only the OS (a privileged sender) can
             deliver SMS_RECEIVED to it. Optional path: harmless if RECEIVE_SMS is
             never granted (the broadcast simply won't be delivered). -->
        <receiver
            android:name=".sms.SmsCheckinReceiver"
            android:exported="true"
            android:permission="android.permission.BROADCAST_SMS">
            <intent-filter>
                <action android:name="android.provider.Telephony.SMS_RECEIVED" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
