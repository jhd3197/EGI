<?xml version="1.0" encoding="utf-8"?>
<!--
  Cleartext (plain HTTP) is denied by default. It is permitted only for the local
  development host and loopback, since community-run EGI sync hubs on a LAN may not
  have TLS. Production servers should use HTTPS; point the native client at an
  https:// URL via the "egi_mesh"/"api_url" preference.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
